import React from 'react'
import { Button } from '@material-ui/core'
import{JotModal} from '../Jot/JotModal'
function SideBar() {
    return (
        <div>
            <Button variant='contained'>Create Jot</Button>
        </div>
    )
}

export default SideBar
